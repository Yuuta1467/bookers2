
<%= render 'list2',user: @user %>

<%= render 'books/list', book: @book %>

<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <%= book.title %>
  <% end %>
  <%= book.body %>
  <%= book.user.name %>
</div>
<% end %>